name: Node CI Pipeline

on:
  workflow_call:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:

      # Checkout Application Code
      - name: Checkout Application Code
        uses: actions/checkout@v4

      # Generate short commit ID
      - name: Set Commit ID
        run: echo "COMMIT_ID=${GITHUB_SHA::7}" >> $GITHUB_ENV

      # Setup Node
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # Install dependencies
      - name: Install Dependencies
        run: npm install

      # Lint code
      - name: Lint Code
        run: npm run lint || echo "No lint configured"

      # Run tests
      - name: Run Tests
        run: npm test || echo "No tests configured"

      # Build application
      - name: Build App
        run: npm run build || echo "No build step"

      # Start app test
      - name: Start App Test
        run: |
          node src/app.js &
          sleep 5

      # Build Docker image
      - name: Build Docker Image
        run: docker build -t node-demo:${{ env.COMMIT_ID }} .

      # Dummy push to registry
      - name: Push Docker Image (Dummy)
        run: |
          IMAGE_NAME=node-demo
          IMAGE_TAG=${{ env.COMMIT_ID }}
          REGISTRY=docker.io/demo-registry

          echo "Pushing Docker image to registry..."
          echo "Image: $REGISTRY/$IMAGE_NAME:$IMAGE_TAG"
          echo "Docker image successfully pushed (simulated)"
